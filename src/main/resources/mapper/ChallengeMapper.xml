<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ctf.platform.mapper.ChallengeMapper">

    <select id="findAll" resultType="com.ctf.platform.entity.Challenge">
        SELECT c.*, cat.name as categoryName 
        FROM challenge c 
        LEFT JOIN category cat ON c.category_id = cat.id
        ORDER BY c.sort_order DESC, c.id ASC
    </select>

    <select id="countAll" resultType="int">
        SELECT count(*) FROM challenge
    </select>

    <select id="findPage" resultType="com.ctf.platform.entity.Challenge">
        SELECT c.*, cat.name as categoryName 
        FROM challenge c 
        LEFT JOIN category cat ON c.category_id = cat.id
        ORDER BY c.sort_order DESC, c.id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countBySearch" resultType="int">
        SELECT count(*) FROM challenge WHERE title LIKE CONCAT('%', #{search}, '%')
    </select>

    <select id="findPageBySearch" resultType="com.ctf.platform.entity.Challenge">
        SELECT c.*, cat.name as categoryName 
        FROM challenge c 
        LEFT JOIN category cat ON c.category_id = cat.id
        WHERE c.title LIKE CONCAT('%', #{search}, '%')
        ORDER BY c.sort_order DESC, c.id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByCategoryId" resultType="int">
        SELECT count(*) FROM challenge WHERE category_id = #{categoryId}
    </select>

    <select id="findPageByCategoryId" resultType="com.ctf.platform.entity.Challenge">
        SELECT c.*, cat.name as categoryName 
        FROM challenge c 
        LEFT JOIN category cat ON c.category_id = cat.id
        WHERE c.category_id = #{categoryId}
        ORDER BY c.sort_order DESC, c.id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findAllCategories" resultType="com.ctf.platform.entity.Category">
        SELECT * FROM category
    </select>

    <select id="findById" resultType="com.ctf.platform.entity.Challenge">
        SELECT * FROM challenge WHERE id = #{id}
    </select>

    <select id="findByCategoryId" resultType="com.ctf.platform.entity.Challenge">
        SELECT c.*, cat.name as categoryName 
        FROM challenge c 
        LEFT JOIN category cat ON c.category_id = cat.id
        WHERE c.category_id = #{categoryId}
        ORDER BY c.sort_order DESC, c.id ASC
    </select>

</mapper>
